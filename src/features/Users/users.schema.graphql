type Query {
    users: [User]
}

type Mutation {
    register(username: String!, email: String!, password: String!): Error
}

type User {
    username: String!
    email: String!
}

enum Role {
    Standalone
    Admin
}

type JwtToken {
    access: String!,
    refresh: String!
}